plugins {
    id 'groovy'
    id 'java-gradle-plugin'
    id "com.gradle.plugin-publish" version "0.13.0"
    id 'maven'
}

compileJava   {
    sourceCompatibility = '1.8'
    targetCompatibility = '1.8'
}

group 'fr.exratio'
version '0.0.20'

repositories {
    jcenter()
}

dependencies {
    implementation gradleApi()
    implementation localGroovy()
}

gradlePlugin {
    plugins {
        jmeDevKitPlugin {
            id = 'fr.exratio.jme-devkit'
            implementationClass = 'fr.exratio.devkit.gradle.JmeDevKitGradlePlugin'
        }
    }
}

pluginBundle {
    website = "https://jmonkeyengine.org"
    vcsUrl = "https://github.com/NyouB/JmeDevKitGradlePlugin"
    description = "Gradle plugin providing integration with the Jme DevKit application."
    tags = [ "jmonkey", "jmonkeyengine" ]

    plugins {
        jmeDevKitPlugin {
            displayName = "JmonkeyEngine DevKit Plugin Gradle Integration"
        }
    }
}
